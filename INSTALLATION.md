📱 **MOBILON** - Интернет-магазин электроники

## 🎯 Что это?

Полнофункциональный интернет-магазин электроники на Django с:
- ✅ Категориями товаров (Телефоны, Ноутбуки, ТВ и т.д.)
- ✅ Товарами с ценами и скидками
- ✅ Поиском и фильтрацией
- ✅ Профессиональным дизайном (темный + синий + красный)
- ✅ WhatsApp интеграцией для заказов
- ✅ Админкой для управления товарами
- ✅ Адаптивным дизайном (мобильные + десктоп)

## 🚀 Быстрый старт

### 1️⃣ Окружение Python
```bash
# Создать виртуальное окружение
python -m venv venv

# Активировать (Windows)
.\venv\Scripts\activate

# Активировать (macOS/Linux)
source venv/bin/activate
```

### 2️⃣ Установка зависимостей
```bash
pip install -r requirements.txt
```

### 3️⃣ Миграции БД
```bash
python manage.py migrate
```

### 4️⃣ Создание администратора
```bash
python manage.py createsuperuser
```

Ответьте на вопросы:
- Username: `admin`
- Email: `admin@mobilon.kg`
- Password: Придумайте пароль

### 5️⃣ Заполнение тестовыми данными
```bash
# Windows
Get-Content populate_db.py | python manage.py shell

# macOS/Linux
python manage.py shell < populate_db.py
```

Этот скрипт добавит:
- 11 категорий
- 19 тестовых товаров

### 6️⃣ Запуск сервера
```bash
python manage.py runserver
```

Откройте в браузере: **http://127.0.0.1:8000/**

---

## 📍 Основные страницы

| Адрес | Описание |
|-------|---------|
| `/` | 🏠 Главная страница |
| `/admin/` | ⚙️ Админка (введите пароль) |
| `/all/` | 📦 Все товары |
| `/categories/` | 📂 Все категории |
| `/category/1/` | 📱 Товары категории (ID 1) |
| `/product/1/` | 🛒 Страница товара (ID 1) |

---

## 🎨 Структура проекта

```
mobilon/
├── db.sqlite3              # База данных
├── manage.py               # Django управление
├── populate_db.py          # 📊 Скрипт для заполнения БД
├── requirements.txt        # 📦 Зависимости
├── README.md              # 📄 Документация
│
├── shop_project/          # ⚙️ Настройки проекта
│   ├── settings.py        # Конфигурация
│   ├── urls.py            # URL маршруты
│   └── wsgi.py            # Production
│
├── store/                 # 📦 Главное приложение
│   ├── models.py          # 📊 Модели (Category, Product)
│   ├── views.py           # 👁️  Представления
│   ├── urls.py            # 🔗 Маршруты
│   ├── admin.py           # ⚙️ Админка
│   └── migrations/        # 📈 Миграции БД
│
├── templates/             # 🎨 HTML шаблоны
│   ├── base.html          # 🔰 Базовый шаблон
│   └── store/
│       ├── homepage.html           # 🏠 Главная
│       ├── product_list.html       # 📦 Список товаров
│       ├── product_detail.html     # 🛒 Деталь товара
│       ├── product_card.html       # 🎴 Карточка товара
│       ├── category_list.html      # 📂 Категории
│       └── category_products.html  # 🏪 Товары в категории
│
├── static/                # 🎨 CSS, JS, изображения
│   └── css/
│       └── style.css      # 🎨 Профессиональные стили
│
└── media/                 # 📸 Загруженные фото товаров
    └── products/
```

---

## 📊 Модели БД

### Category (Категория)
```python
- name           # Название (пример: "Телефоны")
- icon           # Emoji иконка (пример: "📱")
- order          # Порядок на сайте (1 = сверху)
- show_on_homepage  # Показывать на главной (да/нет)
```

### Product (Товар)
```python
- name           # Название товара
- price          # Текущая цена
- old_price      # Старая цена (для скидок)
- description    # Описание
- image          # Фото товара
- category       # Связь с категорией
- brand          # Бренд (Tefal, Ariston, Apple и т.д.)
- available      # В наличии (да/нет)
- is_popular     # Популярный товар (выделяется)
- show_on_homepage  # Показывать на главной
- views_count    # Количество просмотров
- created_at     # Когда добавили
```

---

## 🎨 Дизайн

**Цветовая схема:**
- 🖤 Темно-серый (#1f2937) - фон
- 🔵 Синий (#2563eb) - кнопки, ссылки
- ❤️ Красный (#ef4444) - скидки
- ⚪ Белый - основной фон контента

**Шрифты:**
- `Montserrat` - Заголовки (смелые, дорогие)
- `Roboto` - Основной текст (читаемый, чистый)

**Адаптивность:**
- 📱 Мобильные (до 480px)
- 🖥️ Планшеты (480px - 768px)
- 💻 Десктоп (768px+)

---

## ⚙️ Админка

### Вход в админку:
1. Откройте `http://127.0.0.1:8000/admin/`
2. Логин: `admin`
3. Пароль: тот, что вы установили при создании

### Что можно делать в админке:

#### 📂 Категории:
- ✏️ Добавлять новые категории
- 🎨 Менять иконки (emoji)
- 🔢 Менять порядок (1, 2, 3...)
- 👁️ Показывать/скрывать на главной

#### 🛒 Товары:
- ✏️ Добавлять товары
- 💰 Устанавливать цены и скидки
- 📸 Загружать фото
- ⭐ Отмечать как популярные
- ✅ Указывать наличие
- 📊 Видеть статистику просмотров

---

## 📞 WhatsApp интеграция

На каждом товаре есть кнопка **"WhatsApp: 0550 179 400"**

Когда покупатель нажимает, открывается WhatsApp с готовым сообщением:
```
"Здравствуйте! Хочу заказать iPhone 17 Pro Max"
```

**Чтобы изменить номер:**
1. Откройте `store/views.py`
2. Найдите `whatsapp_number` и `phone_number`
3. Измените на свой номер
4. Сохраните и перезагрузите страницу

Примеры номеров:
- Кыргызстан: `+996550179400`
- Россия: `+79991234567`
- Казахстан: `+77001234567`

---

## 🔍 Поиск и фильтры

На странице "Все товары" можно:
- 🔍 **Искать** по названию, бренду или описанию
- 📂 **Фильтровать** по категориям
- 🔄 **Сбросить** фильтры

Пример поиска:
1. Перейдите на `/all/`
2. В поле поиска введите "iPhone"
3. Нажмите "Найти"

---

## 🚀 Развертывание (Production)

### Для Heroku:
1. Установите Heroku CLI
2. Создайте приложение: `heroku create mobilon`
3. Добавьте PostgreSQL: `heroku addons:create heroku-postgresql:hobby-dev`
4. Разверните: `git push heroku main`

### Для собственного сервера:
1. Используйте Gunicorn: `pip install gunicorn`
2. Запустите: `gunicorn shop_project.wsgi:application`
3. Поставьте Nginx впереди как reverse proxy

---

## 🐛 Решение проблем

### Ошибка: "Static files not found"
```bash
python manage.py collectstatic
```

### Ошибка: "No module named 'django'"
```bash
pip install -r requirements.txt
```

### Ошибка БД
```bash
python manage.py migrate
```

### Забыли пароль администратора
```bash
python manage.py changepassword admin
```

---

## 📝 Как добавить свой товар?

### Способ 1: Через админку (легко)
1. Откройте `/admin/`
2. Нажмите "Products" → "Add product"
3. Заполните форму
4. Загрузите фото
5. Сохраните

### Способ 2: Через код (для программистов)

Отредактируйте `populate_db.py` и добавьте в список:
```python
{
    "name": "Ваш товар",
    "category": "📱 Телефоны",  # Выберите категорию
    "price": 50000,
    "old_price": 60000,  # Опционально
    "description": "Описание товара",
    "brand": "Apple",
    "available": True,
    "is_popular": True,  # Будет выделен
    "show_on_homepage": True,
},
```

Затем запустите:
```bash
Get-Content populate_db.py | python manage.py shell
```

---

## 💡 Советы

### Для лучшего вида:
- ✅ Загружайте квадратные фото (1000x1000px)
- ✅ Используйте JPG или PNG
- ✅ Оптимизируйте размер (< 500KB)

### Категории по популярности:
1. 📱 Телефоны
2. 💻 Ноутбуки
3. 📺 Телевизоры
4. ❄️ Холодильники
5. 🧺 Стиральные машины

---

## 📞 Контакты

**Создан для:** Mobilon - интернет-магазин электроники  
**Телефон:** 0550 179 400  
**WhatsApp:** +996 550 179 400

---

## 📄 Лицензия

MIT License - используйте свободно!
